
  import { ISnippets } from "./interface";

  export default  [{
      body: `
        <Calendar value={value} onChange={handleValueChange} format="YYYY-MM-DD" />
      `,
      prefix: `fcalendar`,
      description: 'fun calendar 受控用法'
    }] as ISnippets[];
